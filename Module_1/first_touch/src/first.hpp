#ifndef AALTO_ELEC_CPP_FIRST
#define AALTO_ELEC_CPP_FIRST
#include <string>
#include <vector>
void Hello();
int LongerLength(const std::string&, const std::string&);
int LargestNumbers(const std::vector<int>&);
#endif
